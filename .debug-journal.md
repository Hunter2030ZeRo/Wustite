# Debug Journal — hot object-call path
Started: 2026-08-18T20:30:00+08:00
Goal: Remove the execute_function_at_depth / numeric leaf interpreter path and make spectral_norm at least 1.20x faster than CPython.

## Environment snapshot (Phase 0)
- Runtime: Rust/Cargo release binary plus CPython 3.14.6 comparison runtime.
- Entry: target/release/wustite bench examples/spectral_norm.py and benchmarks/python_runtime.py.
- Ports / sockets: none.
- Git HEAD: 5ef282dab75e5202bf34b0a700ceda548708fb6d; worktree already dirty with user changes.
- References read: debugging runtimes/rust.md, methodology/00-setup.md, methodology/02-investigate.md.

## Hypotheses
1. [CONFIRMED] Numeric leaf calls rescan and interpret eval_A on every invocation — distinguishing evidence: run 1 records eval_A=676000 and the source walks bytecode in execute_numeric_leaf_call/execute_leaf — if true, fix is: predecode leaf.
2. [CONFIRMED] Nested function entry repeatedly allocates and initializes frames — distinguishing evidence: execute_function_at_depth calls initialize_registers, which allocates a register Vec for each of 5200 part-function calls — if true, fix is: reuse frames.
3. [CONFIRMED] Runtime-call boundaries keep most eval_A work outside native scalar code — distinguishing evidence: eval_A=676000 but helper_calls.call=5468 and native_executions=83 in the release repro — if true, fix is: direct scalar call.

## Failed hypothesis round counter
- Round 1: all three hypotheses confirmed by the existing release repro and source/runtime counters.

## Artifacts to revert
- [ ] `.debug-journal.md` — this journal. Remove after final verification.
- [ ] `/tmp/wustite-hot-call-perf.data` — current post-fix perf capture. Remove after analysis.
- [ ] `/tmp/wustite-inline-perf.data` — post-inline perf capture. Remove after analysis.
- [ ] Source and regression-test edits for the requested fix — retain as deliverables.
- [ ] `src/wvm/jit_runtime.rs` temporary profile diagnostic in transient WXIR error branch. Remove after root-cause capture.

## Findings
- 2026-08-18 release repro: Wustite Cranelift warm median 520.736 ms; CPython median 136.685 ms.
- 2026-08-18 debug run: eval_A=676000, part_A_times_u=2600, part_At_times_u=2600, helper_calls.call=5468, native_executions=83.

## Final fix
- Pending.

---

# Debug Journal — default-stack guest call depth
Started: 2026-08-29T09:30:00+08:00
Goal: Return the typed depth-limit error at guest depth 128 without exhausting the default host test stack.

## Environment snapshot (Phase 0)
- Runtime: Rust 1.97.1 / Cargo 1.97.1, default dev test profile.
- Entry: `cargo test --test call_depth -- --test-threads=1`.
- Ports / sockets: none.
- Git HEAD: shared heavily dirty worktree; unrelated edits preserved.
- Graph: home-entity27th-Wustite, 5091 nodes / 33963 edges, ready, no parse-partial or skipped files.
- References read: OMO debugging Rust runtime, setup, and investigation; OMO programming Rust README; RTK.md.

## Hypotheses
1. [ACTIVE] `execute_instruction` retains a large union of Call-arm locals across recursive guest execution — distinguishing evidence: per-depth stack-address delta and an inline boundary/source toggle — if true, fix is: isolate call frame.
2. [ACTIVE] `execute_function_at_depth` retains a large `FunctionRuntime` or `Frame` local across recursion — distinguishing evidence: `size_of` values plus heap-local toggle changing default-stack outcome — if true, fix is: box proven local.
3. [ACTIVE] `invoke_callable` retains a large cloned `Object` enum payload across recursive invocation — distinguishing evidence: `size_of::<Object>()` and a scope/toggle that drops it before recursive call — if true, fix is: narrow ownership.

## Failed hypothesis round counter
- Round 1: pending.

## Artifacts to revert
- [ ] `.omo/evidence/adaptive-v2/f1-performance/nbody-maximal-advance-cfg/production-timeout-fix/call-depth-fix/` — retained task evidence.
- [ ] `src/wvm.rs` temporary stack-address and type-size diagnostics — remove before final verification.
- [ ] Any temporary `#[inline(never)]` or heap-allocation bisection toggle — remove unless it is the proven minimal final fix.
- [ ] Any isolated Cargo target created for stack artifacts — remove during cleanup.

## Findings
- Pending.

## Final fix
- Pending.

---

# Debug Journal — nbody maximal advance CFG
Started: 2026-08-28T00:00:00+08:00
Goal: Lower one verified `advance` call CFG across its outer, pair, and position loops with one rooted atomic transaction lifetime.

## Environment snapshot (Phase 0)
- Runtime: Rust/Cargo, adaptive-v2 WVM/WXIR with Cranelift and optional LLVM.
- Entry: persistent Runtime integration tests, then the release `wustite run` CLI.
- Ports / sockets: none.
- Git HEAD: d5f28c144615426aa23f5ad5bd837070301d04a9; worktree already heavily dirty with user and peer changes.
- Graph: home-entity27th-Wustite, 5073 nodes / 33683 edges, ready, no parse-partial or skipped files.
- References read: codebase-memory SKILL; OMO programming Rust README; OMO debugging Rust runtime, setup, and investigation references; RTK.md.

## Hypotheses
1. [ACTIVE] `loop_regions().next()` selects the first/leaf loop instead of the largest structurally enclosing loop — distinguishing evidence: selected region entry/block set versus all callee loop region block sets and post-warm machine-entry delta — if true, fix is: maximal containment.
2. [ACTIVE] The outer loop is selected but its exits are lowered as resumable loop exits before the callee `Return` — distinguishing evidence: selected immutable WXIR terminators/deopt recipes and runtime entry/commit deltas — if true, fix is: verified return continuation.
3. [ACTIVE] Maximal selection succeeds, but dynamic transaction descriptors are incomplete for all derived velocity/position lists — distinguishing evidence: direct-storage input descriptors, owner/layout/version receipts, native acceptance, and zero-partial-mutation negatives — if true, fix is: rooted set propagation.

## Artifacts to revert
- [ ] `.omo/evidence/adaptive-v2/f1-performance/nbody-maximal-advance-cfg/` — retained task evidence; remove only if abandoning the task.
- [ ] `/tmp/wustite-nbody-maximal-target` — isolated release target; remove during cleanup.
- [ ] Any temporary runtime instrumentation — journal exact source path before adding; remove before final verification.
- [ ] `src/adaptive_v2/integration/loop_snapshot/cfg/instruction.rs` — temporary selected-loop diagnostic around maximal nested-call lowering; remove after causal capture.
- [ ] `src/adaptive_v2/integration/loop_osr.rs` — temporary verified-preheader candidate/lifecycle diagnostic; remove after causal capture.
- [ ] Requested source and regression-test edits — retain only if focused and strict gates pass.

## Findings
- 2026-08-28: Independent current-source review observed production executable 3 at PC4/PC10 recording and PC109 compiled, with 119872 native entries per `advance` invocation and helper/generic/deopt/exits all zero.

## Final fix
- Pending.
